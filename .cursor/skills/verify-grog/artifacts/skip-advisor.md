# Skip: advisor

Unmet precondition: `/advisor` consults need an authenticated session and a seat. This VM hit the login gate (`Waiting for approval`) and has no `claude` / `agy` / Codex tokens. Dump-rebase 84745de9 / grog 1.0.41. Not a pass.
