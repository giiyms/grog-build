# Skip: advisor

Unmet precondition: `/advisor` is session-scoped. This VM stops on the login gate, so there is no agent session, and native consults would additionally need a provider seat. Not a pass.
