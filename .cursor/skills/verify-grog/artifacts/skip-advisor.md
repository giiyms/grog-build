# Skip: advisor

Unmet precondition: `/advisor` consults need an authenticated session and a seat. This VM hit the login gate (`Waiting for approval`) and has no `claude` / `agy` / Codex tokens. Dump-rebase 036a5d83 / grog 1.0.41. Not a pass.
