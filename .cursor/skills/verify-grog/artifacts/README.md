# verify-grog proof artifacts

This directory holds evidence from skill-driven proofs. `scripts/cleanup.sh` deletes the disposable `$GROG_HOME` and the tmux session; it must not delete these files.

Proven: [identity-and-home](../features/identity-and-home.md) — see `proof.md`, `version.txt`, `doctor-cli.txt`, `tui-login-gate.txt`.
