# Skip: help

Unmet precondition: no authenticated pager session on this VM. After `grog --no-leader` under disposable `GROG_HOME`, the TUI shows the xAI login gate (`Waiting for approval...`), not welcome `Quit`. Typing `/help` then Enter left the same gate (see `help-after-submit.txt` vs `tui-login-gate.txt`). Completing browser login was out of scope. Not a pass.
