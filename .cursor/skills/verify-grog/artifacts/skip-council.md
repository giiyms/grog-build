# Skip: council

Unmet precondition: live `/council` seats need at least one of `claude` on PATH, `agy` on PATH, or Codex tokens under `$GROG_HOME/auth/codex.json`. `grog doctor` reported all three providers `missing`. Dump-rebase 84745de9 / grog 1.0.41. Not a pass.
