[package]
name = "grog-claude-bridge"
version = "0.1.0"
edition.workspace = true
license = "Apache-2.0"
description = "Native Claude Code bridge for grog (spawn claude, stream-json, AskClaude)"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
