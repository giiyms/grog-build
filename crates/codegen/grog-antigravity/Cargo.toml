[package]
name = "grog-antigravity"
version = "0.1.0"
edition.workspace = true
license = "Apache-2.0"
description = "Native Antigravity/agy bridge for grog (spawn agy, protobuf step decode, AskAntigravity)"

[dependencies]
thiserror = { workspace = true }

[lints]
workspace = true
