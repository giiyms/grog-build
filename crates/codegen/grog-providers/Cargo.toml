[package]
name = "grog-providers"
version = "0.1.0"
edition.workspace = true
license = "Apache-2.0"
description = "Grog inference provider registry: model ids, catalogs, and native bridges"

[dependencies]
grog-antigravity = { path = "../grog-antigravity" }
grog-claude-bridge = { path = "../grog-claude-bridge" }
grog-codex = { path = "../grog-codex" }
thiserror = { workspace = true }

[lints]
workspace = true
